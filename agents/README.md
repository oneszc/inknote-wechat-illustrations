# Agent Metadata

`openai.yaml` is optional metadata for OpenAI/Codex-style interfaces.

It provides display text and a default prompt. Claude Code does not require this file and uses the root `SKILL.md` directly.
